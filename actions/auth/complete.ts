import axios from "axios";

const completeSignUp = async ({
  title,
  about,
  subdomain,
}: {
  title: string;
  about: string;
  subdomain: string;
}) => {
  const data = { title, about, subdomain };

  try {
  } catch (error) {}
};
